import React from "react";
import ReactDOM from "react-dom/client";
import HtmlCleanupTool from "./HtmlCleanupTool";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HtmlCleanupTool />
  </React.StrictMode>
);
